{
  "private": true,
  "name": "websocket-server",
  "version": "0.0.1",
  "description": "Simple websocket server with auto response",
  "scripts": {
    "start": "exec java -jar build/libs/messaging-stomp-websocket-0.0.1-SNAPSHOT.jar",
  },
  "dependencies": {
    "ws": "^8.5.0"
  }
}
