{
  "private": true,
  "name": "gql-server",
  "version": "0.0.1",
  "description": "MS7 GraphQL Server",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "lint": "eslint --ext .js,.jsx,.ts,.tsx src/",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@ms7/common": "*",
    "@ms7/logger": "*",
    "apollo-server": "^3.9.0",
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.33",
    "dev-tools": "*",
    "eslint": "^8.16.0",
    "ts-config": "*",
    "ts-node": "^10.8.1",
    "typescript": "^4.6.3"
  }
}
